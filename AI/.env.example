# Olmec AI Core Configuration

# Master server IP (for workers to connect to)
MASTER_IP=127.0.0.1

# API Key for inference
OLMEC_API_KEY=OLMEC_DEV_KEY_99

# Ports
DASHBOARD_PORT=8080
API_PORT=8000

# Weights Path
WEIGHTS_PATH=models/weights_latest.pt
